#!/sbin/openrc-run

description="Closes the splash screen shown on boot"

depend() {    
    after *
    need localmount
}

start() {
    ebegin "Closing fbsplash"

    exec 3<>/.splash.ctrl
    echo "exit" > /.splash.ctrl 2>/dev/null || true

    eend $?
}